# 快速上手

本指南将带你快速了解 Keq 的基本用法，从安装到发送第一个请求。

## 安装

import InstallPackagesCode from '@site/docs/components/_install-packages-code.mdx'
import CompatibilityTable from '@site/docs/components/_compatibility-table.mdx'

<InstallPackagesCode packages="keq" />

**兼容性**

<CompatibilityTable />

## 发送你的第一个请求

使用 Keq 发送 HTTP 请求非常简单，只需调用对应的方法并使用 `await` 等待结果：

```typescript
import { request } from "keq"

const cats = await request.get("/cats")

console.log(`我的可爱的猫咪们有：${cats.map(cat => cat.name).join(", ")}`)
```

:::tip 自动解析响应体
Keq 会根据响应头的 `Content-Type` 自动解析响应体（[详细说明](./responses/auto-parsing)），省去手动调用 `.json()` 的麻烦。
:::
