import InstallPackagesCode from '@site/docs/components/_install-packages-code.mdx'
import ExecPackageCommandCode from '@site/docs/components/_exec-package-command-code.mdx'

# 在 NestJS 中使用 Keq

本文介绍如何在 NestJS 项目中集成 Keq，以及如何使用 OpenAPI 自动生成类型安全的客户端代码。

## 安装依赖


<InstallPackagesCode packages="keq @keq-request/cli @keq-request/nestjs" />

## 注册 KeqModule

在应用的根模块中注册 `KeqModule`：

```typescript title="src/app.module.ts"
import { Module } from '@nestjs/common'
import { KeqModule } from '@keq-request/nestjs'
import { validateStatusCode } from '@keq-request/exception'

@Module({
  imports: [
    KeqModule.register({                                       // [!code ++]
      middlewares: [                                           // [!code ++]
        // 配置全局中间件                                         [!code ++]
        validateStatusCode(),                                  // [!code ++]
        KeqRouter.host('example.com', setHeader('Authorization', 'Bearer YOUR_TOKEN_HERE')), // [!code ++]                                                    // [!code ++]
      ]                                                        // [!code ++]
    }),                                                        // [!code ++]

    /* ...其他模块... */
  ]
})
export class AppModule {}
```

注册后即可在 Service 中通过依赖注入使用 `KeqRequest`：

```typescript title="src/app.service.ts"
import { Injectable } from '@nestjs/common'
import { KeqRequest } from 'keq'

@Injectable()
export class AppService {
  constructor(private readonly request: KeqRequest) {}

  async getCats() {
    const cats = await this.request.get('https://api.example.com/cats')
    return cats
  }
}
```

## configureKeqMiddleware 进阶配置

除了通过 `register()` 直接配置全局中间件外，Keq 也提供了类似 NestJS `MiddlewareConsumer` 的编程式接口，支持**按路由注册中间件**以及**排除特定路由**。

### 基本用法

在任意 NestJS 模块中实现 `configureKeqMiddleware` 方法：

```typescript title="src/some-feature.module.ts"
import { Module } from '@nestjs/common'
import { KeqModule, type KeqMiddlewareConsumer, KEQ_ROUTES } from '@keq-request/nestjs'

@Module({
  imports: [KeqModule],
})
export class SomeFeatureModule {
  configureKeqMiddleware(consumer: KeqMiddlewareConsumer): void {
    consumer
      .apply(loggingMiddleware)
      .forRoutes(KEQ_ROUTES.ALL)
  }
}
```

### apply() — 注册中间件

`apply()` 方法接收 Keq 中间件或 NestJS 风格的中间件类：

```typescript
import { type KeqNestMiddleware, type KeqExecutionContext, type KeqNext } from '@keq-request/nestjs'

// NestJS 风格中间件类（支持依赖注入）
class AuthMiddleware implements KeqNestMiddleware {
  use(ctx: KeqExecutionContext, next: KeqNext): Promise<void> {
    // 鉴权逻辑
    return next()
  }
}

consumer.apply(AuthMiddleware, loggingMiddleware).forRoutes(KEQ_ROUTES.ALL)
```

:::tip 中间件类型
`apply()` 支持两种类型：
- **`KeqMiddleware`**：普通函数中间件 `(ctx, next) => void`
- **`Type<KeqNestMiddleware>`**：NestJS 类中间件（支持依赖注入）
:::

### forRoutes() — 指定路由范围

`forRoutes()` 控制中间件对哪些请求生效：

```typescript
// 全局生效
consumer.apply(loggingMiddleware).forRoutes(KEQ_ROUTES.ALL)

// 仅对特定路径生效（支持 picomatch 通配符）
consumer.apply(authMiddleware).forRoutes({
  pathname: '/api/**',  // 仅匹配 /api/* 路径
  method: 'POST',       // 仅 POST 请求
})

// 仅对特定模块的请求生效
consumer.apply(catMiddleware).forRoutes(CatServiceModule)
```

`forRoutes()` 接收三种路由目标：

| 类型 | 说明 |
|------|------|
| `KEQ_ROUTES.ALL` | 匹配所有请求 |
| `KeqRouteInfo` | `{ host?, method?, pathname? }` — 按 host / 方法 / 路径过滤 |
| `KeqModuleClass` | 生成的 NestJS 模块类（仅该模块的请求生效） |

:::info 路由匹配
`KeqRouteInfo` 使用 [picomatch](https://github.com/micromatch/picomatch) 作为路径匹配引擎。当同时指定多个条件时（如 `pathname` + `method`），它们为 **AND** 关系，即所有条件都满足时才命中。
:::

### exclude() — 排除特定路由

`exclude()` 可在 `forRoutes()` 之前调用，排除中间件作用范围内的特定路由：

```typescript
// 全局应用 auth 中间件，但排除 health check 和公开接口
consumer.apply(AuthMiddleware)
  .exclude(
    { pathname: '/health' },
    { pathname: '/public/**' },
    { method: 'OPTIONS' },
  )
  .forRoutes(KEQ_ROUTES.ALL)
```

也可以在路由级中间件中使用，实现子路径排除：

```typescript
// 对 /api/** 应用日志中间件，但排除 /api/internal/**
consumer.apply(LoggingMiddleware)
  .exclude({ pathname: '/api/internal/**' })
  .forRoutes({ pathname: '/api/**' })
```

:::tip exclude 语义
- **单条 `KeqRouteInfo` 内的多个条件**（如 `{ pathname: '/api/**', method: 'GET' }`）为 **AND** 关系
- **多次调用 `exclude()` 或多条参数**为 **OR** 累积关系（任何一条匹配即排除）
- `exclude()` 返回 `KeqMiddlewareConfigProxy`，支持在 `forRoutes()` 之前链式调用
:::

## 使用 OpenAPI 生成 NestJS 模块

`@keq-request/cli` 支持从 OpenAPI 文档生成 NestJS 模块，提供完整的类型定义和依赖注入支持。

### 配置生成模式

在 `.keqrc.ts` 中设置 `mode` 为 `nestjs-module`：

```typescript title=".keqrc.ts"
import { defineKeqConfig, FileNamingStyle } from '@keq-request/cli'

export default defineKeqConfig({
  mode: 'nestjs-module',   // [!code ++]
  outdir: "./src/apis",
  rendering: {
    fileNamingStyle: FileNamingStyle.snakeCase,
  },
  modules: {
    catService: "./cat-service-swagger.json",
  },
})
```

运行生成命令：

<ExecPackageCommandCode command="keq build" />

### 注册生成的模块

将生成的模块导入到应用中：

```typescript title="src/app.module.ts"
import { Module } from '@nestjs/common'
import { KeqModule } from '@keq-request/nestjs'
import { CatServiceModule } from './apis/cat_service/cat_service.module' // [!code ++]

@Module({
  imports: [
    // 必须注册 KeqModule
    KeqModule.register({
      // 配置全局中间件
      middlewares: [
        validateStatusCode(),
      ]
    }),

    // 注册生成的模块（fork 自全局 KeqRequest，继承全局中间件）
    CatServiceModule.register({                                   // [!code ++]
      // 模块级中间件配置                                            [!code ++]
      middlewares: [                                              // [!code ++]
        setBaseUrl('https://cat-api.example.com'),                // [!code ++]
        appendHeader('Authorization', 'Bearer YOUR_TOKEN_HERE'),  // [!code ++]
      ]                                                           // [!code ++]
    }),                                                           // [!code ++]
  ]
})
export class AppModule {}
```

:::tip 中间件执行顺序
请求发送时，中间件的执行顺序为：全局中间件 → 模块级中间件。
:::

### 隔离模式

如果你不希望生成的模块继承全局中间件，可以设置 `isolate: true` 创建独立的 `KeqRequest` 实例：

```typescript
@Module({
  imports: [
    CatServiceModule.register({
      isolate: true,   // 不继承全局中间件，创建独立实例
      middlewares: [
        setBaseUrl('https://cat-api.example.com'),
        appendHeader('Authorization', 'Bearer YOUR_TOKEN_HERE'),
      ]
    }),
  ]
})
export class SomeFeatureModule {}
```

:::info 实例获取策略
- 未设置 `isolate`（默认）：从全局 `KeqModule` 提供的 `KeqRequest` fork，继承全局中间件
- `isolate: true`：创建全新的 `KeqRequest` 实例，不继承任何全局配置

如果未注册 `KeqModule` 且未设置 `isolate: true`，模块会自动创建独立实例并输出警告。
:::

### 使用生成的客户端

```typescript title="src/cat.service.ts"
import { Injectable } from '@nestjs/common'
import { CatServiceClient } from './apis/cat_service/cat_service.client'

@Injectable()
export class CatService {
  constructor(private readonly catServiceClient: CatServiceClient) {}

  async getCats() {
    const response = await this.catServiceClient.getCats<200>()
    return response.body
  }

  async getCatById(id: string) {
    const response = await this.catServiceClient.getCatById<200>({ id })
    return response.body
  }
}
```
